---
name: resume-format
description: 按用户专属简历模板（WXX 简历模版）生成排版一致的 .docx 简历。严格复刻模板的字体（全宋体，仅联系方式/求职意向西文用 Times New Roman）、字号（姓名 14pt、分区标题 13pt、条目 12pt、正文 10.5pt）、单倍行距、段前段后、字符间距、页边距（上下 1cm、左右 1.27cm、A4）、分区标题底部黑色下划线、条目时间右对齐、要点•前缀悬挂缩进。加粗规则：姓名、分区标题、条目行左侧、所有标签（核心工作/项目背景/技术栈/主要职责/项目成果/专业技能/综合素质）加粗；基本信息、时间、正文、要点不加粗。当用户要求"写简历 / 生成简历 / 简历排版 / 简历格式 / 按我的模板写简历 / 帮我做简历 / CV / resume"时使用；用户提到要产出与某份简历模板排版一致的文件时也使用。
agent_created: true
---

# 简历排版（resume-format）

生成与用户模板 `简历模版.docx` **排版完全一致**的简历 .docx 文件。
模板副本位于本 skill 的 `assets/简历模版.docx`，生成结果应与其视觉一致。

## 何时使用

- 用户要求写/生成/排版一份简历，且希望沿用既有模板的格式。
- 用户提到"和之前那版一样""按模板来""字体间距大小都按模板"等。

## 工作流程

1. **收集内容**：向用户确认或整理简历内容（基本信息、教育经历、实习经历、项目经历、校园经历、个人技能）。内容不足时先询问，不要编造。
2. **组织 JSON**：参照 `scripts/resume.example.json` 的结构，把内容填入 JSON：
   - 顶层：`name`（姓名）、`basic_info`（性别/年龄）、`contact`（手机/邮箱）、`job_intent`（求职意向）、`sections`（分区数组）。
   - 分区：`title`（分区名）、`items`（内容项数组）。
   - item 类型：
     - `entry`：条目行，`left` 为加粗 12pt 的名称，`center` 为居中角色名（加粗 12pt，如"项目负责人"），`right` 为时间（10.5pt **不加粗**）。教育条目无 `center`。
     - `body`：普通正文段（**不加粗**，全宋体）。教育经历的"主修课程："等用此类型。
     - `label_body`：`label` 标签（**加粗**）+ `text` 正文（不加粗）。项目经历标签（项目背景/技术栈/代码仓库/主要职责/项目成果）和实习/校园的"核心工作："用此类型。
     - `bullet`：悬挂缩进要点行（"• "前缀，不加粗）。主要职责/项目成果后跟的分点内容用此类型。
3. **生成 docx**：运行生成脚本
   ```bash
   python scripts/build_resume.py --content <你的内容.json> --output <输出路径>.docx
   ```
   若环境无 python-docx，先 `pip install python-docx`。
4. **交付**：把生成的 .docx 呈现给用户。

## 关键格式速查（详见 references/format-spec.md）

- 页面：A4，上下边距 1cm，左右边距 1.27cm。
- **字体**：全宋体。仅联系方式和求职意向的西文用 Times New Roman。
- **字号**：姓名 14pt 加粗居中；分区标题 13pt 加粗 + 底部 0.5pt 黑色单线；条目行 12pt 加粗；正文 10.5pt。
- **行距**：全文单倍行距。
- **求职意向**：居中，在手机号下面。求职意向后有一个空段落。
- 首个分区标题段前=0（前面有空段落间隔），后续标题段前段后各 0.5 行。
- 条目时间用右对齐制表位推到行尾，**不加粗**。角色名用居中制表位推到页面中间。
- 要点行：• 前缀 + 悬挂缩进 2 个全角字符（21pt），**不加粗**。
- 分区顺序固定：教育经历 → 实习经历 → 项目经历 → 校园经历 → 个人技能。

## 加粗规则（严格按模板，不要自作主张）

| 加粗 | 不加粗 |
|---|---|
| 姓名（14pt） | 基本信息行（性别/年龄） |
| 分区标题（13pt） | 联系方式行 |
| 条目行左侧名称+角色（12pt） | 求职意向行 |
| 实习/校园"核心工作："标签 | 条目行时间 |
| 项目经历标签（项目背景/技术栈/代码仓库/主要职责/项目成果） | 教育经历正文（主修课程/荣誉奖项/资格证书） |
| 个人技能标签（1.专业技能：/2.综合素质：） | 项目经历正文和要点（• 前缀） |

## 生成说明

- 输出必须是 .docx 文件（Word/WPS 兼容），不要输出 PDF/HTML 代替，除非用户另行要求。
- 生成脚本是标准 python + python-docx，Windows 下用系统 Python 或 managed Python 均可。
- 若用户提供了新的真实内容（如具体学校、公司名），直接用真实内容替换占位符即可。
